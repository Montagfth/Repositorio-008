# Configuración básica de Peeps
spring.application.name=peeps-backend
server.port=8080

# Configuración de CORS simple
cors.allowed.origins=*
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed.headers=*
